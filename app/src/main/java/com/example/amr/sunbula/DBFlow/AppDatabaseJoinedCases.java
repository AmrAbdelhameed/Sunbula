package com.example.amr.sunbula.DBFlow;

import com.raizlabs.android.dbflow.annotation.Database;

@Database(name = AppDatabaseJoinedCases.NAME, version = AppDatabaseJoinedCases.VERSION)
public class AppDatabaseJoinedCases {
    public static final String NAME = "JoinedCasesProfile"; // we will add the .db extension

    public static final int VERSION = 1;
}
